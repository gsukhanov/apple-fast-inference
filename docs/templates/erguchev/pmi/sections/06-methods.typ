#import "../../../shared/typst/core.typ": paragraph, project-name, project-name-english, project-summary, project-type

= МЕТОДЫ ИСПЫТАНИЙ

== Проверка требований к технической документации

#paragraph[
  Проверка осуществляется путем визуального анализа состава и содержания представленного комплекта документов на соответствие стандартам ЕСПД и требованиям технического задания. В ходе проверки подтверждается наличие следующих документов:
  - Техническое задание: проверка полноты описания целей, задач и функциональных требований к реализации тензорного ядра и AMX-исполнения.
  - Пояснительная записка: проверка наличия описания архитектуры тензоров, алгоритмов матричного умножения и обоснования выбора путей исполнения (CPU/AMX).
  - Программа и методика испытаний: проверка наличия описанных сценариев тестирования корректности вычислений и замера производительности.
  - Текст программы: проверка наличия исходного кода всех модулей (core, nn, backend) и заголовочных файлов.
  - Руководство оператора: проверка инструкций по сборке библиотеки, интеграции API и запуску эталонных тестов.
]

== Проверка требований к интерфейсу

#paragraph[
  Поскольку разрабатываемый продукт представляет собой библиотеку (CNN inference API library), проверка интерфейса заключается в анализе C++ API на соответствие требованиям к доступности и удобству использования. Проверяются следующие аспекты:
  - Интерфейс создания тензоров: проверка корректности работы функций создания тензоров с заданной формой (shape), типом данных (dtype) и режимом вычислений.
  - Интерфейс доступа к данным: проверка методов чтения и записи данных в тензор, а также работы итераторов.
  - Интерфейс выполнения операций: проверка единообразия вызовов для матричного умножения и сверточных слоев независимо от выбранного бэкенда (CPU или AMX).
  - Диагностический интерфейс: проверка вывода информативных сообщений об ошибках при несовместимости размерностей матриц или недоступности аппаратного ускорения.
]

== Проверка требований к функциональным характеристикам

#paragraph[
  Функциональные характеристики проверяются путем выполнения серии тестов, включающих как модульное тестирование отдельных операций, так и сквозное тестирование на архитектуре LeNet.
]

=== Проверка корректности матричного умножения (CPU vs AMX)

#paragraph[
  *Исходные данные:* Два тензора (матрицы) случайных размерностей, совместимых для умножения, с вещественными значениями.
  *Действия:* 
  1. Выполнить умножение матриц с использованием эталонного CPU-пути.
  2. Выполнить умножение тех же матриц с использованием AMX/Accelerate-пути.
  3. Вычислить разность между результатами двух вычислений.
  *Ожидаемый результат:* Результаты вычислений на CPU и AMX должны совпадать с точностью до заданной численной погрешности.
  *Критерий успешности:* Максимальная абсолютная разность между соответствующими элементами результирующих тензоров не превышает допустимый порог (epsilon).
]

=== Проверка обработки ошибок размерностей

#paragraph[
  *Исходные данные:* Два тензора, размерности которых делают операцию матричного умножения математически невозможной (например, количество столбцов первой матрицы не равно количеству строк второй).
  *Действия:* Вызвать функцию матричного умножения для данных тензоров.
  *Ожидаемый результат:* Программа не должна аварийно завершаться (crash); API должен вернуть диагностическую ошибку или выбросить исключение, указывающее на несовместимость форм.
  *Критерий успешности:* Фиксация корректного сообщения об ошибке размерности.
]

=== Проверка работы сквозного вывода (Inference) на модели LeNet

#paragraph[
  *Исходные данные:* Веса предобученной сети LeNet (файл mnist_lenet.json) и тестовые изображения из набора `inputs_test`.
  *Действия:* 
  1. Загрузить веса модели в структуры тензоров.
  2. Прогнать тестовые изображения через всю цепочку слоев (Conv2D $\to$ ReLU $\to$ AvgPool2D $\to$ Flatten $\to$ Linear $\to$ Softmax).
  3. Сравнить полученный класс с истинной меткой изображения.
  *Ожидаемый результат:* Модель корректно классифицирует изображения, возвращая индекс наиболее вероятного класса.
  *Критерий успешности:* Совпадение предсказанного класса с эталонным для большинства тестовых примеров.
]

=== Сравнение производительности путей исполнения

#paragraph[
  *Исходные данные:* Набор матриц больших размерностей, типичных для сверточных слоев VGG-уровня.
  *Действия:* 
  1. Замерить время выполнения операции умножения на CPU-эталоне.
  2. Замерить время выполнения той же операции на AMX/Accelerate-бэкенде.
  3. Построить сравнительный график.
  *Ожидаемый результат:* Время выполнения на AMX/Accelerate-пути должно быть существенно ниже, чем на CPU-пути.
  *Критерий успешности:* Подтвержденный прирост производительности при использовании аппаратного ускорения Apple Silicon.
  \
  TODO: вставить скрин или график сравнения производительности CPU и AMX.
]
