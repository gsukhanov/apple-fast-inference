#import "../../../shared/typst/core-sukhanov.typ": paragraph, project-name, project-name-english, project-summary, project-type

= Выполнение программы

== Установка программы

#paragraph[
  Для развертывания и настройки среды исполнения библиотеки #project-name необходимо подготовить аппаратную платформу на базе Apple Silicon (M-серия), так как функционал AMX-исполнения привязан к специфическим инструкциям данной архитектуры.

  Процесс установки и настройки включает следующие этапы:
  1. Клонирование исходного кода проекта из репозитория.
  2. Установка среды разработки Xcode (версии 15.0 или выше) для компиляции C++ кода.
  3. Настройка окружения Python для работы со скриптом `pt2json.py`, который используется для конвертации весов моделей из формата PyTorch в JSON-формат, поддерживаемый библиотекой.
  4. Компиляция проекта с использованием стандартных средств сборки (например, CMake или Xcode Build), при этом в настройках компилятора должны быть указаны флаги оптимизации для архитектуры arm64 и поддержка SIMD-инструкций NEON.
  5. Размещение файлов весов (например, `mnist_lenet.json`) и тестовых изображений в директории `tests/weights/` и `tests/inputs_test/` соответственно.
]

== Запуск программы и работа с приложением

#paragraph[
  Поскольку #project-name представляет собой API-библиотеку (CNN inference API library), взаимодействие с ней осуществляется через запуск скомпилированных исполняемых файлов тестов (`lenet_test`) или бенчмарков (`lenet_benchmark`). Управление параметрами работы происходит на этапе конфигурации кода или через передачу аргументов запуска.
]

=== Запуск интеграционных тестов (проверка точности)

#paragraph[
  Данный сценарий используется для проверки корректности работы реализации сверточных слоев, im2col-преобразования и функций активации.

  Действия оператора:
  1. Скомпилировать файл `lenet_test.cpp`.
  2. Запустить исполняемый файл, указав путь к файлу весов `mnist_lenet.json` и тестовым изображениям из папки `inputs_test`.

  Реакция программы:
  Программа загружает веса модели, инициализирует тензоры, выполняет прямой проход (inference) по тестовому набору данных и выводит результат классификации для каждого изображения.

  Ожидаемый результат:
  Вывод в консоль предсказанного класса для каждого тестового изображения. Сравнение результатов между CPU-исполнением и AMX-исполнением должно показывать идентичные или близкие значения (с учетом погрешности квантования).

  TODO: вставить скриншот вывода консоли с результатами тестов lenet_test
]

=== Проведение бенчмаркинга (сравнение производительности)

#paragraph[
  Сценарий предназначен для измерения времени выполнения операций и сравнения эффективности CPU/NEON и AMX режимов.

  Действия оператора:
  1. Скомпилировать файл `lenet_benchmark.cpp`.
  2. Запустить программу с выбором режима вычислений (CPU или AMX) и указанием разрядности данных (вещественные или квантованные).
  3. Зафиксировать параметры эксперимента: размер входного тензора, размер пакета (batch size) и тип данных.

  Реакция программы:
  Программа выполняет многократный запуск итераций сверточного слоя или всей сети LeNet, замеряя время выполнения каждой операции с помощью высокоточного таймера.

  Ожидаемый результат:
  Вывод в консоль среднего времени выполнения операции в миллисекундах или микросекундах, а также расчет пропускной способности.

  TODO: вставить скриншот или график сравнения производительности CPU vs AMX
]

=== Конвертация весов модели

#paragraph[
  Сценарий подготовки данных для работы библиотеки с использованием внешнего скрипта.

  Действия оператора:
  1. Запустить скрипт `pt2json.py` через интерпретатор Python.
  2. Передать на вход файл модели PyTorch (.pth) и указать путь для сохранения результирующего .json файла.

  Реакция программы:
  Скрипт считывает тензоры весов и смещений из модели PyTorch, нормализует их (в случае квантования) и записывает в структурированный JSON-файл.

  Ожидаемый результат:
  Создание файла `mnist_lenet.json`, который может быть считан классом `serialization` библиотеки для инициализации слоев нейронной сети.
]

=== Работа с квантованными представлениями

#paragraph[
  Сценарий проверки работы сети в режиме пониженной разрядности.

  Действия оператора:
  1. В конфигурации инициализации модели выбрать квантованный тип данных (согласно `fastinf/core/dtype.hpp`).
  2. Запустить выполнение сети на тестовых данных.

  Реакция программы:
  Библиотека использует специализированные пути исполнения для квантованных данных, применяя параметры масштабирования (scaling factors) для корректного приведения типов.

  Ожидаемый результат:
  Выполнение инференса с меньшим потреблением памяти и потенциально более высокой скоростью работы при сохранении приемлемой точности классификации.
]
