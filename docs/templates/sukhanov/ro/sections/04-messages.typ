#import "../../../shared/typst/core.typ": paragraph, project-name, project-name-english, project-summary, project-type

= СООБЩЕНИЯ ОПЕРАТОРУ

== Тексты сообщений оператору

#paragraph[
  Поскольку библиотека представляет собой программный интерфейс (API) для высокопроизводительных вычислений, сообщения оператору выводятся в стандартный поток ошибок (stderr) или через механизмы логирования при возникновении критических сбоев в процессе инициализации или исполнения нейронной сети.

  Ключевые категории сообщений включают:
  
  - *Ошибки инициализации и загрузки*: сообщения о невозможности найти или прочитать файл с весами модели (например, при некорректном пути к `.json` файлу), а также ошибки десериализации структуры сети.
  - *Ошибки совместимости типов*: предупреждения о несоответствии разрядности входных данных и выбранного режима исполнения (вещественный или квантованный).
  - *Ошибки выделения памяти*: уведомления о невозможности выделить непрерывный блок памяти для тензоров при работе с большими размерами пакетов (batch size) или слоями с большим числом каналов.
  - *Ошибки конфигурации тензоров*: сообщения о несоответствии размерностей при операциях свертки (conv2d) или матричного умножения (matmul), например, если размер входного тензора не соответствует ожидаемому размеру ядра или шага.
  - *Информационные сообщения о режиме исполнения*: уведомления о выбранном бэкенде вычислений (CPU/NEON или AMX) и используемом представлении данных.
]

== Действия оператора в исключительных ситуациях

#paragraph[
  При возникновении исключительных ситуаций оператор должен предпринять следующие действия в зависимости от типа проблемы:

  - *Некорректные входные данные или веса*: 
    При получении ошибки загрузки модели необходимо проверить целостность JSON-файла с весами и соответствие структуры сети (количество слоев, размерности) архитектуре, заложенной в коде (например, LeNet). Проверить правильность путей к файлам в конфигурации запуска.

  - *Ошибка несовместимости архитектуры процессора*: 
    Если при попытке использования AMX-исполнения возникает сбой или ошибка инициализации, оператору следует переключить режим вычислений на CPU/NEON, который является эталонным и резервным режимом исполнения для всех поддерживаемых устройств Apple Silicon.

  - *Превышение лимитов памяти*: 
    В случае ошибок выделения памяти (out of memory) необходимо уменьшить размер входного пакета (batch size) или оптимизировать размерность входных тензоров.

  - *Низкая точность квантованного исполнения*: 
    При обнаружении значительного расхождения в результатах между вещественной и квантованной реализациями, оператору следует проверить параметры масштабирования (scaling factors) и разрядность квантования, согласованную с моделью.

  - *Нестабильная работа или падение (crash)*: 
    В случае критического сбоя оператор должен зафиксировать параметры эксперимента (размер входа, тип данных, разрядность, режим вычислений) и предоставить данные логи через систему отслеживания ошибок или репозиторий проекта.
]
